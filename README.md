# OpenAI Realtime Console with Firecrawl

An inspector and interactive API reference for the OpenAI Realtime API with [Firecrawl](https://firecrawl.dev) integration. Install with `npm i`, start with `npm start`, and access at `localhost:3000`.
